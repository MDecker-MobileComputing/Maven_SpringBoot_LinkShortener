/**
 * Dieses Paket enthält Datenmodellklassen inkl. Exception-Klassen.
 * <br><br>
 *
 * Namenskonventionen:
 * <ul>
 * <li>Alle Klassen, die mit {@code Rest} anfangen, werden für HTTP-Antworten
 *     nach JSON serialisiert.</li>
 * <li>Alle Klasse, die mit {@code Kafka} anfangen, werden für Kafka-Nachrichten
 *     nach JSON serialisiert.</li>
 * </ul>
 */
package de.eldecker.dhbw.spring.urlshortener.ms_urldefinition.model;
